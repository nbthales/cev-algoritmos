algoritmo "somador"
var
N, soma: Inteiro
resp: Caractere
inicio

soma <- 0
Repita
   Escreva ("Digite o valor: ")
   Leia (N)
   soma <- soma +n
   Escreva("Voce quer continuar? [S/N]")
   Leia(resp)
Ate(resp="N")
EscrevaL("A soma de todos os valores foi ", soma)
fimalgoritmo
